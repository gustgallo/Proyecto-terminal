# En producción no es necesario VITE_API_URL porque frontend y API comparten dominio.
# api.ts utiliza por defecto /api/v1.
#
# Solo si la API estuviera en otro dominio:
# VITE_API_URL=https://api.ejemplo.com/api/v1
